[project]
name = "log-service"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
log-service = "log_service.__main__:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
